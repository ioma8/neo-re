#ifndef WRITE_OR_DIE_ALPHA_EXPORT_H
#define WRITE_OR_DIE_ALPHA_EXPORT_H

#include <stdbool.h>
#include <stdint.h>

#include "app_state.h"

uint32_t alpha_export_append_session(const WodEditor_t* editor, uint32_t slot);

#endif
